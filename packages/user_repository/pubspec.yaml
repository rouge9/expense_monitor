name: user_repository
description: The Repo that handles users
publish_to: "none"
version: 0.0.1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5
  firebase_auth: ^4.10.0
  cloud_firestore: ^4.17.3
  google_sign_in: ^6.2.1
  flutter_bloc: ^8.1.3
  uuid: ^4.4.0

  expense_repository:
    path: ../expense_repository

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
